import { StyleSheet, Text, View } from "react-native";
const Connect = () => {
  return (
    <View>
      <Text>Connect</Text>
    </View>
  );
};

export default Connect;

const styles = StyleSheet.create({});
