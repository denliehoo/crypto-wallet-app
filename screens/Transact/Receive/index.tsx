import { StyleSheet, Text, View } from "react-native";
const Receive = () => {
  return (
    <View>
      <Text>Receive</Text>
    </View>
  );
};

export default Receive;

const styles = StyleSheet.create({});
