import { StyleSheet, Text, View } from "react-native";
const SendConfirmationScreen = () => {
  return (
    <View>
      <Text>SendConfirmationScreen</Text>
    </View>
  );
};

export default SendConfirmationScreen;

const styles = StyleSheet.create({});
